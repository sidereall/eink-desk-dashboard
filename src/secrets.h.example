// secrets.h.example - template for secrets.h
// Changing networks means reflashing. Only loadCredentials() reads these.

#pragma once

#define WIFI_SSID      "your-network-name"
#define WIFI_PASSWORD  "your-network-password"
